import { useState } from "react";
import DraftingEditor from "../TextTranslation/SimplifiedEditor/DraftingEditor";
import { Box } from "@mui/material";

export default function TranslationPlanEditorMuncher({ metadata }) {
  const [modified, setModified] = useState(false);

  return (
    <Box sx={{ p: 2 }}>
      <p> ici </p>
    </Box>
  );
}
